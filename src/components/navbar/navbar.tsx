import React, {FC} from 'react';
import { Layout } from 'antd';

const Navbar: FC = () => {
    const { Header } = Layout;

    return (
        <Header>Header</Header>
    )
}

export default Navbar;